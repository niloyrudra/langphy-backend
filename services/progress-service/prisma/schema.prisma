datasource db {
    provider = "postgresql"
}

generator client {
    provider = "prisma-client-js"
}

model UserProgress {
    userId              String @id
    totalActivities     Int
    lastActivityDate    DateTime
}